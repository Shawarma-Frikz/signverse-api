#backend dependencies
fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
python-multipart
pydantic-settings
resend
itsdangerous
bcrypt==4.0.1
email-validator
slowapi

#ml dependencies
torch
mediapipe
opencv-python
numpy
tqdm
scikit-learn
tensorflow>=2.16.0,<3  # Python 3.12 compatible; 2.12.0 only supports Python 3.8-3.11
# tflite-runtime is optional; the app falls back to full TensorFlow when unavailable
# tflite-runtime not installed on Windows via pip for this Python version;
# the app falls back to full TensorFlow (tf.lite.Interpreter) when tflite-runtime
# is unavailable, so functionality is unchanged.

#others
kaggle