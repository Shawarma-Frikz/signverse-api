#backend dependencies
fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
python-multipart
pydantic-settings
resend
itsdangerous
bcrypt==4.0.1
email-validator
slowapi

#ml dependencies
torch
mediapipe
opencv-python
numpy
tqdm
scikit-learn
tensorflow==2.12.0  # Requires Python 3.8-3.11 on Windows
# tflite-runtime is optional; the app falls back to full TensorFlow when unavailable
# tflite-runtime not installed on Windows via pip for this Python version;
# the app falls back to full TensorFlow (tf.lite.Interpreter) when tflite-runtime
# is unavailable, so functionality is unchanged.

#others
kaggle